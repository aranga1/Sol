name: CI

on:
  push:
    branches: [main, 'feature/**', 'feat/**', 'fix/**', 'prd/**']
  pull_request:
    branches: [main]

jobs:
  # ── iOS simulator build (no signing required) ─────────────────────────────
  ios-build:
    name: iOS build (simulator)
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode 16
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '16.2'

      - name: Install xcodegen
        run: brew install xcodegen

      - name: Generate BuildSecrets.swift (stub for build)
        run: |
          python3 -c "
          content = '''enum BuildSecrets {
              static let githubIssuesPAT = \"ci-stub\"
              static let githubRepo = \"aranga1/Sol\"
          }
          '''
          open('ios/Sol/BuildSecrets.swift', 'w').write(content)
          "

      - name: Resolve Swift packages
        working-directory: ios
        run: xcodegen generate

      - name: Resolve Swift packages
        working-directory: ios
        run: |
          xcodebuild -resolvePackageDependencies \
            -project Sol.xcodeproj \
            -scheme Sol \
            -clonedSourcePackagesDirPath .spm-cache

      - name: Build for simulator
        working-directory: ios
        run: |
          xcodebuild build \
            -project Sol.xcodeproj \
            -scheme Sol \
            -destination 'platform=iOS Simulator,name=iPhone 16' \
            -configuration Debug \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            | xcpretty --color || xcodebuild build \
              -project Sol.xcodeproj \
              -scheme Sol \
              -destination 'platform=iOS Simulator,name=iPhone 16' \
              -configuration Debug \
              CODE_SIGN_IDENTITY="" \
              CODE_SIGNING_REQUIRED=NO \
              CODE_SIGNING_ALLOWED=NO

  ios-tests:
    name: iOS unit tests (simulator)
    runs-on: macos-15
    needs: ios-build
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode 16
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '16.2'

      - name: Install xcodegen
        run: brew install xcodegen

      - name: Generate BuildSecrets.swift (test stub)
        run: |
          python3 -c "
          content = '''enum BuildSecrets {
              static let githubIssuesPAT = \"test-pat-stub\"
              static let githubRepo = \"aranga1/Sol\"
          }
          '''
          open('ios/Sol/BuildSecrets.swift', 'w').write(content)
          "

      - name: Generate Xcode project
        working-directory: ios
        run: xcodegen generate

      - name: Resolve Swift packages
        working-directory: ios
        run: |
          xcodebuild -resolvePackageDependencies \
            -project Sol.xcodeproj \
            -scheme Sol \
            -clonedSourcePackagesDirPath .spm-cache

      - name: Run unit tests
        working-directory: ios
        run: |
          xcodebuild test \
            -project Sol.xcodeproj \
            -scheme SolTests \
            -destination 'platform=iOS Simulator,name=iPhone 16' \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            2>&1 | grep -E "Test Suite|BUILD|error:" | head -20

  # ── Daemon unit tests ─────────────────────────────────────────────────────
  daemon-tests:
    name: Daemon tests (Python)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e packages/solidrag
          pip install -r daemon/requirements.txt
          pip install pytest pytest-asyncio

      - name: Run tests
        run: PYTHONPATH=. pytest daemon/tests/ -v
